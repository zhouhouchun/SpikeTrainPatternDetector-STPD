import AppKit
import SwiftUI

enum STPDAppTheme {
    static let accent = Color(red: 0x45 / 255.0, green: 0x8B / 255.0, blue: 0x6E / 255.0)
    static let nsAccent = NSColor(red: 0x45 / 255.0, green: 0x8B / 255.0, blue: 0x6E / 255.0, alpha: 1)
}
